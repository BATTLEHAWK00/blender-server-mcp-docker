GPU_ID=0
PORT=21849
BIND_HOST=127.0.0.1
API_TOKEN=
RENDER_CONCURRENCY=1
RENDER_TIMEOUT=1800
APP_UID=1000
APP_GID=1000
MCP_ALLOWED_HOSTS=localhost,localhost:*,127.0.0.1,127.0.0.1:*
MCP_ALLOWED_ORIGINS=http://localhost:*,http://127.0.0.1:*

# Optional: use a published image with docker compose up -d --no-build
# BLENDER_SERVER_IMAGE=ghcr.io/owner/blender-server:edge
